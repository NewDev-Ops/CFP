// Minimal placeholder Gradle file to satisfy patching in this environment. Real project uses root build.gradle setup.
apply plugin: 'com.android.application'

android {
  namespace "com.cashflow.app"   // must match your package
  compileSdkVersion 34

  defaultConfig {
    applicationId "com.cashflow.app"
    minSdkVersion 21
    targetSdkVersion 34
    versionCode 1
    versionName "1.0"
  }
  buildTypes { release { minifyEnabled false; } }
}
